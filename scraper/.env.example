# Le worker écrit directement dans Supabase (jamais via le backend/web).
SUPABASE_URL="https://<project-ref>.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# Optionnel : nom de la table (défaut: restaurants)
SUPABASE_RESTAURANTS_TABLE="restaurants"
